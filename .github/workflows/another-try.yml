name: Convert CV to PDF

on:
  push:
    branches:
        - trial

jobs:
  convert:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.0'
        extensions: mbstring, dom
        tools: composer

    - name: Install PHP dependencies
      run: composer install

    - name: List installed PHP packages
      run: ls -l vendor

    - name: Check for specific function or class existence
      run: php -r "require 'vendor/autoload.php'; echo class_exists('SomeClass') ? 'exists' : 'does not exist';"

    - name: Convert Markdown/YAML to PDF
      run: php convert-to-pdf.php

    - name: Upload PDF
      uses: actions/upload-artifact@v2
      with:
        name: cv.pdf
        path: cv.pdf
